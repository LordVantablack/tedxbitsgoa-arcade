services:
  arcade:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - path: .env.local
        required: false
    volumes:
      - .:/app
      - arcade_node_modules:/app/node_modules
      - arcade_wrangler:/app/.wrangler

volumes:
  arcade_node_modules:
  arcade_wrangler:
